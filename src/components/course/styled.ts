import { Box, styled } from "@mui/material";

export const CoverCourse = styled(Box)({
    height: "220px",
    position: "relative"
})

export const Item = styled(Box)({
    position: "relative",
})
export const Price = styled(Box)({
    display: "flex",
    alignItems: "center",
    gap: 2,
})

export const Body = styled(Box)({
    padding: "1rem",
    display: "flex",
    flexDirection: "column",
    gap: 2
})

export const Duration = styled(Box)({
    display: "flex",
    alignItems: "center",
    gap: 2,
})